/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "https://picsum.photos",
      "https://cdn.discordapp.com/",
    ],
  },
};

module.exports = nextConfig;
